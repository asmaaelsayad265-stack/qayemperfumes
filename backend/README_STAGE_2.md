# Backend (Laravel API) - Stage 2 Notes

> مرحلة skeleton + auth placeholder فقط (لا migrations / لا endpoints).

## الهدف
- إنشاء هيكلة Laravel backend.
- تجهيز Auth للـ Admin كـ placeholder (بدون تنفيذ DB migrations).
- تجهيز structure للـ Controllers/Models/Routers بدون endpoints فعلية.

## القادم
- إنشاء Laravel project داخل `backend/`.
- تجهيز الملفات الأساسية:
  - Controllers skeleton
  - Models skeleton
  - routes/api.php placeholder
- إعداد CORS/ENV placeholders للتكامل لاحقًا.

