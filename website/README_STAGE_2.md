# Website (Next.js) - Stage 2 Notes

> مرحلة skeleton + تصميم (لا business logic / لا CRUD / لا API endpoints).

## الهدف
- إكمال بنية Next.js (App Router) مع RTL وثيم Luxury Black/Gold.
- إنشاء layouts ومكونات مشتركة فقط.
- ترك صفحات placeholder فارغة لواجهات العملاء و /admin.

## تم الآن (قبل Stage 2)
- تم إنشاء هيكل صفحات placeholder أساسي داخل `src/app`.
- تم إعداد Tailwind configuration جزئيا.

## القادم
- ضبط `app/layout.tsx` لدعم RTL والخط العربي.
- إضافة theme tokens + components base (Buttons/Cards/Headers/Footer) بدون منطق.
- إضافة `app/(public)/...` و `app/(admin)/...` كتنظيم إن لزم بدون تغيير المسارات النهائية.

